[tool.pytest.ini_options]
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]

[tool.ruff]
extend-exclude = [
    ".pip-audit-cache",
    ".venv",
    ".venv-linux",
    "owasp-pysec",
]
